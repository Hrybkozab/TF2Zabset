# Steam API Key — https://steamcommunity.com/dev/apikey
# Local: domain localhost | Netlify prod: domain tf2zabset.netlify.app
STEAM_API_KEY=1DC0F07AB48A8E0ED7B02CD187A1DDE1

# Auth session signing (generate: openssl rand -base64 32)
SESSION_SECRET=m1x5v0Wl2r3mR2qK6g7xWQ3W7P8xgY8fY423oHnJ2JQ=

# Neon — use the *pooler* connection string (host ends with -pooler)
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# Then run: npm run db:migrate

# Frontend
VITE_LOADOUTS_API_URL=/.netlify/functions/weapons

# Optional: separate Express API (Railway/Render). If set, auth uses that server instead of Netlify functions.
# VITE_API_BASE=https://your-api.example.com
